#include "stdafx.h"
#include <iostream>
#include <vector>
#include <algorithm>
#include <string>
using namespace std;

//Take a number and return its string(check-written) form.
class CheckWriter{
public:
	string convertToWords(int number);
};